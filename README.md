# linear-algebra.c
Simple c library for Vector, Matrix and Transform math.

## Documentation
Run following command in you terminal
```c
make
```
this make script will compile static library, after compilation copy header files from `include` directory and library files from `libs` directory into you project directory, then by compiling link you'r project with the library.

You can find examples in `examples` directory.
